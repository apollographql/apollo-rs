type Dog {
  name: String!
}

type Query {
  dog: Dog
}

query {
  dog {
    ...undefinedFragment
  }
}
type Dog { name: String! } type Query { dog: Dog } query { dog { ...undefinedFragment } }
type Dog{name:String!} type Query{dog:Dog} query{dog{...undefinedFragment}}
