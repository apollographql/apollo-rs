type Query implements NamedEntity {
  name: String
}

interface NamedEntity {
  name: String
}

interface NamedEntity {
  name: String
}
type Query implements NamedEntity { name: String } interface NamedEntity { name: String } interface NamedEntity { name: String }
type Query implements NamedEntity{name:String} interface NamedEntity{name:String} interface NamedEntity{name:String}
