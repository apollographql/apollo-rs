query getName {
  cat
}

query getName {
  cat
}

type Query {
  cat: String
}
query getName { cat } query getName { cat } type Query { cat: String }
query getName{cat} query getName{cat} type Query{cat:String}
