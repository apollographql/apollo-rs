directive @nonRepeatable on SCHEMA

type Query {
  viewCount: Int!
}

schema @nonRepeatable {
  query: Query
}

extend schema @nonRepeatable
directive @nonRepeatable on SCHEMA type Query { viewCount: Int! } schema @nonRepeatable { query: Query } extend schema @nonRepeatable
directive @nonRepeatable on SCHEMA type Query{viewCount:Int!} schema@nonRepeatable{query:Query} extend schema@nonRepeatable
