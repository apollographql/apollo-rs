subscription sub {
  __typename
}

type Subscription {
  __typename: String
}

type Query {
  x: Int
}
subscription sub { __typename } type Subscription { __typename: String } type Query { x: Int }
subscription sub{__typename} type Subscription{__typename:String} type Query{x:Int}
