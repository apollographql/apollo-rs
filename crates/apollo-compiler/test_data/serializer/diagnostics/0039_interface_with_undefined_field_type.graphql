type Query {
  width: Int
  img: Url
}

interface Connection {
  relationship: Person
}
type Query { width: Int img: Url } interface Connection { relationship: Person }
type Query{width:Int img:Url} interface Connection{relationship:Person}
