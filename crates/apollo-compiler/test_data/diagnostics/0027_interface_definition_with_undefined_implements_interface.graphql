type Query implements Resource {
  id: ID!
  width: Int
}

interface Resource {
  id: ID!
  width: Int
}

interface Image implements Resource & Url {
  id: ID!
  url: String
  width: Int
  thumbnail: String
}