query Q($label: String!) {
    me {
        ... @defer(label: $label) {
            id
        }
    }
}

directive @defer(
    if: Boolean! = true
    label: String
) on FRAGMENT_SPREAD | INLINE_FRAGMENT

type Query {
    me: User
}

type User {
    id: ID
}
