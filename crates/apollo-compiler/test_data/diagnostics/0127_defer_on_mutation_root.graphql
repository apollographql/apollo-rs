mutation M {
    ... @defer {
        mutationField
    }
}

directive @defer(
    if: Boolean! = true
    label: String
) on FRAGMENT_SPREAD | INLINE_FRAGMENT

type Query {
    field: String
}

type Mutation {
    mutationField: String
}
